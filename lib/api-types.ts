// The web consumes the contract from a vendored copy of the generated types
// (produced by corpopay-api's `contract:generate`). Refresh with `npm run types:fetch`.
export type { components, paths } from "@/contract/api-types";
